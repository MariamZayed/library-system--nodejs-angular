export interface BasicAdmin{
    firstName:string,
    lastName:string,
    password:string,
    email:string,
    hiredate:string,
    birthdate:string,
    image?:string,// ? means optional
    salary:Number,
}